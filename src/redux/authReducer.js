const initialState = {
  email: "",
  login: '',
  userId: '',
  isAuth: false
};

const profileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default profileReducer;

export {

};
